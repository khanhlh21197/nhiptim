import 'package:flutter/material.dart';

class BaoHanh extends StatelessWidget {
  const BaoHanh({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Chức năng đang phát triển', style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),),
    );
  }
}
